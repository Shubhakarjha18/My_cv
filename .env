APP_NAME=Sanatani_coder
APP_ENV=production
APP_KEY=base64:PkZGN/+qnFlVh2Qh0SY3xUG5TfrUN1V8Px0z0s+wYr8=
APP_DEBUG=false  # Set to false in production
APP_TIMEZONE=UTC
APP_URL=http://mycv-production-9347.up.railway.app  # Update with your actual production URL

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=oYwKTlahRgJEOtkhkqpNZnXlYVnkfkRy

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1  # Change this if you're using Memcached

# Redis settings
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1  # Change this if you're using Redis
REDIS_PASSWORD=null
REDIS_PORT=6379
# REDIS_URL=redis://127.0.0.1:6379  # Uncomment if using Redis

# Mail settings
MAIL_MAILER=smtp  # Change to smtp if using SMTP
MAIL_HOST=smtp.mailtrap.io  # Change to your SMTP host
MAIL_PORT=2525
MAIL_USERNAME=null  # Provide if using SMTP
MAIL_PASSWORD=null  # Provide if using SMTP
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS settings
# AWS_ACCESS_KEY_ID=your_aws_access_key  # Provide your AWS access key if using AWS services
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key  # Provide your AWS secret key if using AWS services
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=your_bucket_name  # Provide your S3 bucket name if using AWS S3
# AWS_USE_PATH_STYLE_ENDPOINT=false

# Optional environment variables that are not needed currently
# DB_CACHE_CONNECTION=database  # or null if not used
# DB_CACHE_LOCK_CONNECTION=database  # or null if not used
# DB_CACHE_LOCK_TABLE=cache_locks  # or null if not used
# MEMCACHED_PERSISTENT_ID=null  # or provide a value if needed
# MEMCACHED_USERNAME=null  # or provide if needed
# MEMCACHED_PASSWORD=null  # or provide if needed
# DYNAMODB_ENDPOINT=null  # or provide if using DynamoDB
# LOG_SLACK_WEBHOOK_URL=null  # or provide if using Slack logging
# PAPERTRAIL_URL=null  # or provide if using Papertrail
# PAPERTRAIL_PORT=null  # or provide if using Papertrail
# MAIL_URL=null  # or provide if using a mail service
# SQS_SUFFIX=null  # or provide if using SQS
# POSTMARK_TOKEN=null  # or provide if using Postmark
# RESEND_KEY=null  # or provide if using Resend
# SLACK_BOT_USER_OAUTH_TOKEN=null  # or provide if using Slack
# SLACK_BOT_USER_DEFAULT_CHANNEL=null  # or provide if using Slack

# SESSION_CONNECTION=null  # or provide if using a session store
# SESSION_STORE=null  # or provide if using a session store
# SESSION_SECURE_COOKIE=false  # Set to true if using HTTPS

VITE_APP_NAME="${APP_NAME}"
